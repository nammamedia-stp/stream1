# StreamPulse Environment Configuration
NODE_ENV=production
JWT_SECRET=change_this_to_a_secure_random_key_in_production_129841824

# Storage Configuration ('postgres' or 'json')
STORAGE_MODE=postgres

# Database Configuration (required if STORAGE_MODE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=streampulse_admin
DB_PASSWORD=streampulse_secure_db_pass_19824
DB_NAME=streampulse

# Optional features
DB_FALLBACK=false
AI_ENABLED=false
GEMINI_API_KEY=

# Initial Administrator Credentials (used on first startup to seed the admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeThisImmediately

# Network & Endpoint Configurations
DOMAIN_NAME=
IS_LOCAL_ENV=
